version: "3.9"

services:
  nginx-proxy:
    image: nginx:1.27-alpine
    container_name: neutrino-nginx-proxy
    ports:
      - "8880:8080"
    volumes:
      - ./apps/neutrino-proxy-dev.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
